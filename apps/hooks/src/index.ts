/**
 * @nexus/hooks - Claude Code lifecycle hooks
 *
 * Hooks:
 * - SessionStart  - Notify API on session start
 * - PreToolUse    - Pass-through
 * - PostToolUse   - Pass-through
 * - Stop          - Pass-through
 * - SessionEnd    - Notify API on session end
 *
 * Install: bun run setup
 * Uninstall: bun run setup:uninstall
 */

export {};
